import SwiftUI

// Temporary skeleton — replaced by SpecterApp.swift in Phase 3.1
@main
struct PlaceholderApp: App {
    var body: some Scene {
        WindowGroup {
            Text("Specter scaffold OK").padding()
        }
    }
}
